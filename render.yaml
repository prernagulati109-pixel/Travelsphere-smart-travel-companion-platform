services:
  - type: web
    name: travelsphere-backend
    runtime: node
    rootDir: ./backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
